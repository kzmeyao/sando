---
import ContentContainer from "./ContentContainer.astro";
---

<header
  class="bg-white pt-16 md:pt-24 pb-8 md:pb-10 textured relative overflow-hidden"
>
  <ContentContainer>
    <div class="flex justify-between items-baseline">
      <a
        href="/"
        class="font-display font-bold text-3xl text-neutral-700 underline decoration-wavy decoration-brand-500 underline-offset-3 decoration-2"
      >
        sando
      </a>
      <button
        id="about-toggle"
        class="text-xs text-neutral-400 hover:text-neutral-600 transition-colors uppercase tracking-wide cursor-pointer relative"
      >
        about
      </button>
    </div>

    <div
      id="about-content"
      class="grid grid-rows-[0fr] transition-[grid-template-rows] duration-500 ease-in-out"
    >
      <div class="overflow-hidden">
        <div
          class="pt-8 grid grid-cols-1 md:grid-cols-2 gap-8 font-serif text-neutral-500 text-xs leading-relaxed"
        >
          <div class="md:col-start-2">
            <p class="pb-4">
              Named after the humble egg salad <em>sando</em> from my first trip to
              Japan, this website is a digital archive of my travels. I approach photography
              with a documentary mindset, focused on preserving scenes as they were
              experienced. Just as food can return you to a moment in time, these
              images serve as my own memory anchors.
            </p>
            <div
              class="flex justify-between items-center pt-4 border-t border-neutral-100"
            >
              <div>
                Want more? Find me on
                <a
                  href="https://www.instagram.com/kzmeyao"
                  target="_blank"
                  class="hover:text-neutral-700 transition-colors underline"
                >
                  Instagram
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </ContentContainer>
</header>

<style>
  #about-toggle {
    background-image: linear-gradient(
      var(--color-brand-500),
      var(--color-brand-500)
    );
    background-repeat: no-repeat;
    background-size: 0% 100%;
    background-position: left;
    transition:
      background-size 0.3s ease-in-out,
      color 0.2s ease-in-out;
  }

  #about-toggle.is-active {
    background-size: 100% 100%;
    color: var(--color-neutral-700);
  }
</style>

<script>
  const toggle = document.getElementById("about-toggle");
  const content = document.getElementById("about-content");

  toggle?.addEventListener("click", () => {
    const isExpanded = content?.classList.contains("grid-rows-[1fr]");

    if (isExpanded) {
      content?.classList.remove("grid-rows-[1fr]");
      content?.classList.add("grid-rows-[0fr]");
      toggle?.classList.remove("is-active");
    } else {
      content?.classList.remove("grid-rows-[0fr]");
      content?.classList.add("grid-rows-[1fr]");
      toggle?.classList.add("is-active");
    }
  });
</script>
